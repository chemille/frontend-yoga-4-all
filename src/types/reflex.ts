export interface ReflexList {
    reflex_id: number;
    title: string;
    videos: Array<string>;
    education: string;
  };

  export interface MultipleReflexes {
    id: number;
    title: string;
  }